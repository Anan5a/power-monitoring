# Local test configuration for Mosquitto.
# Anonymous connections are allowed for easy local development/testing.
# For production, switch to the HTTP auth backend (see mosquitto.conf).

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
autosave_interval 30

log_dest stdout
log_type error
log_type warning
log_type information
connection_messages true
